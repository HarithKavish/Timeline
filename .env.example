# Base URL of the deployed Timeline News worker (workers/news). Defaults to
# the wrangler dev server when unset, so `npm run dev` works against a local
# `npm --prefix workers/news run dev` without any configuration.
VITE_NEWS_API_BASE=http://localhost:8787
